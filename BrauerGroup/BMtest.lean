-- import Mathlib

-- theorem no : False := (by decide +kernel : (open Classical in if Type = Type then 0 else 0) ^ 1 ≠ 0) (by simp)

-- def FLT : Prop := ∀ x y z n : Nat, 0 < x → 0 < y → 0 < z → 2 < n → x ^ n + y ^ n ≠ z ^ n

-- theorem proofFLT : FLT := no.elim
-- theorem proofNotFLT : ¬FLT := no.elim

-- #print axioms no
